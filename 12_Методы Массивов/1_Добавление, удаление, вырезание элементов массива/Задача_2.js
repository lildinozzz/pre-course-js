const numbers = [0, 1, 2, 3, "x", 5, 6, 7, 8, 9, "x", 11, 12, 13, 14];

let firstXIndex;
let secondXIndex;
// здесь найдите индексы первого и второго иксов с помощью цикла for

// здесь используйте метод slice для вырезания элементов между первым и вторым иксом (не включая их)
const numbersPart;

console.log(numbersPart);
