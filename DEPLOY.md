# Инструкция по развертыванию

## 📦 Подготовка проекта

### 1. Установка зависимостей

```bash
npm install
```

### 2. Проверка работоспособности

```bash
# Запуск всех тестов
npm test

# Запуск программы
npm start
```

## 🔐 Развертывание на GitHub (приватный репозиторий)

### Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com)
2. Нажмите на кнопку "New repository" (или "+")
3. Заполните информацию:
   - **Repository name**: `pre-course-tasks`
   - **Description**: `Задачи подготовительного курса по JavaScript`
   - **Visibility**: ✅ Выберите **Private** (приватный репозиторий)
   - **Initialize**: Не добавляйте README, .gitignore или лицензию (они уже есть в проекте)
4. Нажмите "Create repository"

### Шаг 2: Инициализация Git и первый коммит

```bash
# Перейдите в директорию проекта
cd /Users/vandan112/Desktop/pre-course-tasks

# Инициализация Git (если еще не сделано)
git init

# Добавление всех файлов в staging
git add .

# Создание первого коммита
git commit -m "Initial commit: Добавлены решения задач с тестами Jest"

# Переименование ветки в main (если нужно)
git branch -M main
```

### Шаг 3: Связывание с удаленным репозиторием

```bash
# Замените <username> на ваш GitHub username
git remote add origin https://github.com/<username>/pre-course-tasks.git

# Проверка удаленного репозитория
git remote -v
```

### Шаг 4: Отправка кода на GitHub

```bash
# Первая отправка кода
git push -u origin main

# При запросе введите ваши учетные данные GitHub
# Рекомендуется использовать Personal Access Token вместо пароля
```

### Создание Personal Access Token (если требуется)

1. Перейдите в Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token"
3. Выберите права доступа: `repo` (полный доступ к приватным репозиториям)
4. Скопируйте токен и используйте его вместо пароля при push

## 🌐 Развертывание на CodeSandbox

### Метод 1: Импорт из GitHub (Рекомендуется)

1. Перейдите на [CodeSandbox](https://codesandbox.io/)
2. Нажмите "Create" → "Import from GitHub"
3. Вставьте URL вашего GitHub репозитория:
   ```
   https://github.com/<username>/pre-course-tasks
   ```
4. CodeSandbox автоматически:
   - Склонирует репозиторий
   - Установит зависимости из `package.json`
   - Настроит окружение Node.js

### Метод 2: Прямая ссылка

Используйте следующий формат URL:
```
https://codesandbox.io/s/github/<username>/pre-course-tasks
```

### Метод 3: Ручная загрузка файлов

1. Перейдите на [CodeSandbox](https://codesandbox.io/)
2. Создайте новый sandbox: "Create" → "Node.js"
3. Загрузите все файлы проекта через интерфейс
4. CodeSandbox автоматически установит зависимости

### Настройка в CodeSandbox

После импорта проекта в CodeSandbox:

1. **Запуск тестов:**
   - Откройте терминал в CodeSandbox
   - Выполните: `npm test`

2. **Запуск программы:**
   - Выполните: `npm start`

3. **Настройка вывода:**
   - Откройте консоль браузера (F12) для просмотра результатов `console.log`

## 🔄 Обновление проекта

### Добавление новых изменений в GitHub

```bash
# Проверка статуса изменений
git status

# Добавление измененных файлов
git add .

# Создание коммита
git commit -m "Описание изменений"

# Отправка на GitHub
git push
```

### Синхронизация с CodeSandbox

CodeSandbox автоматически синхронизируется с GitHub. Для обновления:
1. Откройте ваш sandbox
2. Нажмите кнопку "Refresh" или перезагрузите страницу
3. CodeSandbox подтянет последние изменения из GitHub

## 🧪 Проверка работоспособности

### Локально

```bash
# Установка зависимостей
npm install

# Запуск тестов
npm test

# Запуск программы
npm start
```

### На CodeSandbox

1. Откройте терминал
2. Выполните `npm test` для проверки тестов
3. Выполните `npm start` для запуска программы

## ✅ Чеклист развертывания

- [ ] Локально установлены зависимости (`npm install`)
- [ ] Все тесты проходят успешно (`npm test`)
- [ ] Проект работает локально (`npm start`)
- [ ] Создан приватный репозиторий на GitHub
- [ ] Код отправлен на GitHub (`git push`)
- [ ] Проект импортирован в CodeSandbox
- [ ] CodeSandbox успешно установил зависимости
- [ ] Тесты работают на CodeSandbox
- [ ] Программа запускается на CodeSandbox

## 🆘 Решение проблем

### Проблема: Git не инициализирован
```bash
git init
```

### Проблема: Ошибка при установке зависимостей
```bash
# Очистка кэша npm
npm cache clean --force

# Удаление node_modules и повторная установка
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Тесты не проходят
```bash
# Запуск тестов с подробным выводом
npm test -- --verbose
```

### Проблема: Доступ запрещен при push на GitHub
- Используйте Personal Access Token вместо пароля
- Проверьте права доступа токена (должен быть включен `repo`)

### Проблема: CodeSandbox не видит приватный репозиторий
- Авторизуйтесь на CodeSandbox через GitHub
- Предоставьте CodeSandbox доступ к приватным репозиториям

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте все шаги в чеклисте
2. Прочитайте раздел "Решение проблем"
3. Проверьте логи ошибок в терминале
4. Убедитесь, что все зависимости установлены

---

**Успешного развертывания! 🚀**

