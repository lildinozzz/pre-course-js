/**
 * Задача 3: Вывод мотивирующего сообщения
 * Выводит в консоль сообщение "Я стану лучшим разработчиком!"
 */
function task3() {
  console.log("Я стану лучшим разработчиком!");
}

// Экспортируем функцию для тестирования
if (typeof module !== "undefined" && module.exports) {
  module.exports = task3;
}

// Запускаем функцию, если файл выполняется напрямую
if (require.main === module) {
  task3();
}
